// @xyz/xy-react — internal component library (demo stand-in)
// Uses vanilla-extract internally for CSS-in-JS. The app never
// imports esbuild or vanilla-extract directly — this library does.
export const Button = ({ children }) => children;
export const Card = ({ children }) => children;
